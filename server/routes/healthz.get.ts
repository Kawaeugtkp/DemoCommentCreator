// =====================================================================
//  ヘルスチェック用エンドポイント（GET /healthz）
//  - 認証不要・依存なしで 200 を返すだけの軽量ルート
//  - ロードバランサ（ECS Express Mode の ALB / Lightsail Containers）の
//    ヘルスチェックパスに指定する。`/` は 302（ログインへ誘導）のため不可。
// =====================================================================
export default defineEventHandler(() => {
  return { status: 'ok' }
})
